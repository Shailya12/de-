rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated users can read or write visitor photos
    match /visitors/{visitorId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
